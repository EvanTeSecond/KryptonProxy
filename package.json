{
  "name": "cloudflare-uv-proxy",
  "version": "1.0.0",
  "description": "Ultraviolet proxy for Cloudflare Workers",
  "main": "src/index.js",
  "scripts": {
    "build": "node build.js",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "@tomphttp/bare-client": "^2.0.0",
    "ultraviolet": "^2.0.1"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
